name: Автомерж для релизных веток 1

on:
  pull_request:
    types: [opened, reopened]

jobs:
  merge-rc-branches:
    runs-on: ubuntu-latest
    steps:
      - name: Получение веток
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            console.log(github)
